<h1>All Patients</h1>
<p>displays a link to each patient's show page and the total number of appointments they have.</p>
<ul>
  <% @patients.each do |patient| %>
  <li>Name: <%= link_to patient.name, patient_path(patient) %></li>
  <li>Number of Appointments: <%= patient.appointment_count %></li>
  <% end %>
</ul>
